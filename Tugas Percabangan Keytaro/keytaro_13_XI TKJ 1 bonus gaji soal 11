# Mengambil nilai performa karyawan dari pengguna
performa = int(input("Masukkan nilai performa karyawan (1 hingga 5): "))

# Menghitung gaji tahunan (misalnya, gaji = 2200000)
gaji = 2200000

# Menghitung bonus berdasarkan nilai performa
if performa == 5:
    bonus = 0.20 * gaji
elif performa == 4:
    bonus = 0.10 * gaji
elif performa == 3:
    bonus = 0.05 * gaji
elif performa == 2:
    bonus = 0.02 * gaji
else:
    bonus = 0

# Mencetak bonus tahunan
print("Bonus tahunan:", bonus)
